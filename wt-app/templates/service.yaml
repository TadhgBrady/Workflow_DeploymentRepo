apiVersion: v1
kind: Service
metadata:
  name: {{ include "wt-app.fullname" . }}
  labels:
    {{- include "wt-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wt-app.selectorLabels" . | nindent 4 }}
