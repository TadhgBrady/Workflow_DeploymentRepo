apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: year4-project-local-apps
  namespace: monitoring
  labels:
    app.kubernetes.io/name: year4-project-local-apps
    app.kubernetes.io/part-of: year4-project-observability
spec:
  namespaceSelector:
    matchNames:
      - year4-project-local
  selector:
    matchExpressions:
      - key: app
        operator: In
        values:
          - admin-bl-service
          - auth-service
          - customer-bl-service
          - customer-db-access-service
          - job-bl-service
          - job-db-access-service
          - maps-access-service
          - notification-service
          - user-bl-service
          - user-db-access-service
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
      scrapeTimeout: 10s
      honorLabels: true
      relabelings:
        - sourceLabels:
            - __meta_kubernetes_service_label_app
          targetLabel: service
        - targetLabel: environment
          replacement: local