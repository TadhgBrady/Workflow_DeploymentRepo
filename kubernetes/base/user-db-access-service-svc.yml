apiVersion: v1
kind: Service
metadata:
  name: user-db-access-service
  labels:
    app: user-db-access-service
spec:
  type: ClusterIP
  selector:
    app: user-db-access-service
  ports:
  - port: 80
    targetPort: 8001
    protocol: TCP
    name: http
  sessionAffinity: None