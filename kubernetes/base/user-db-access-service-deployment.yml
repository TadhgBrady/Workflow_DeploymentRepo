apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-db-access-service
  labels:
    app: user-db-access-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-db-access-service
  template:
    metadata:
      labels:
        app: user-db-access-service
    spec:
      serviceAccountName: user-db-access-sa
      containers:
      - name: user-db-access-service
        image: bencev04/4th-year-proj-tadgh-bence:user-db-access-service-latest
        ports:
        - containerPort: 8001
          name: http
          protocol: TCP
        envFrom:
        - secretRef:
            name: app-secrets
        - configMapRef:
            name: app-config
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DATABASE_URL
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/v1/ready
            port: 8001
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 10