apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-bl-service
  labels:
    app: user-bl-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-bl-service
  template:
    metadata:
      labels:
        app: user-bl-service
    spec:
      serviceAccountName: user-bl-service-sa
      containers:
      - name: user-bl-service
        image: bencev04/4th-year-proj-tadgh-bence:user-bl-service-latest
        ports:
        - containerPort: 8004
          name: http
          protocol: TCP
        envFrom:
        - secretRef:
            name: app-secrets
        - configMapRef:
            name: app-config
        env:
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: redis-credentials
              key: USER_BL_REDIS_URL
        - name: USER_SERVICE_URL
          value: "http://user-db-access-service"
        - name: AUTH_SERVICE_URL
          value: "http://auth-service"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/v1/health
            port: 8004
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/v1/ready
            port: 8004
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 10