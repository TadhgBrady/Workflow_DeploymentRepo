apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-bl-service
  labels:
    app: admin-bl-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: admin-bl-service
  template:
    metadata:
      labels:
        app: admin-bl-service
    spec:
      serviceAccountName: admin-bl-service-sa
      containers:
      - name: admin-bl-service
        image: bencev04/4th-year-proj-tadgh-bence:admin-bl-service-latest
        ports:
        - containerPort: 8008
          name: http
          protocol: TCP
        env:
        - name: ENVIRONMENT
          value: "production"
        - name: LOG_LEVEL
          value: "INFO"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8008
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8008
          initialDelaySeconds: 5
          periodSeconds: 5
       